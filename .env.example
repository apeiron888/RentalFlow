## Database (MongoDB)
# Base connection string read by pkg/config with the RENTALFLOW_ prefix already applied.
# Example for local standalone MongoDB
RENTALFLOW_DATABASE_URI=mongodb://username:password@localhost:27017
# Logical database name each service will use (defaults to {service}_db if omitted)
RENTALFLOW_DATABASE_NAME=rentalflow

# Example for MongoDB Atlas SRV connection (uncomment and adjust as needed)
# RENTALFLOW_DATABASE_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
# RENTALFLOW_DATABASE_NAME=rentalflow
